# WorkLikeRico 落地任务书

2026-09-09 更新：按用户补充收紧首期范围，使用现有 GPT / MiMo 订阅，简单需求直接解决。

2026-09-11 证据口径修订：下表的部署、渠道与发布结果是对应历史阶段的记录，不代表当前在线或总目标完成。逐项未完成范围以 [验收证据](acceptance-results.md) 和 [实施状态](status.md) 为准；后续真实业务中的多轮纠偏不能被早期合成场景通过覆盖。

## 交付目标

一个本地 Hermes 助手，用你现有的能力处理和跟进工作；一个 GitHub 项目 `worklikerico` 维护自建 skills 与插件；一份短工作协议说明怎样执行、验收和提醒。

直接使用 Hermes 原生消息入口、cron 和任务看板。GPT / MiMo 走原生支持的订阅入口，不额外开发代理、调度器、任务数据库或控制台。

最新优先级（2026-09-09 用户纠偏）：统一集合是为了降低安装和管理成本，并让 Hermes 按需复用长期验证的能力；研究记录的核心是识别满意结果和工作方式，改善项目预期制定及方案完整性，减少偏离与返工。主线转向满意证据、预期与方案回放，再落实到 Hermes。设计—验收插件过度触发和性能比较后置；日常监测仍暂停。安装数量和专项技能样例不能代替这个目标。

本阶段按[满意标准、预期制定与 Hermes 整合任务书](habits-plugin-rebuild.md)推进，明确区分用户纠正、可接受条件、实际结果和明确认可，再判断应复用或修改哪些能力。

## 任务清单

| 任务 | 负责人 | 验收 | 当前状态 |
|---|---|---|---|
| 调查现有能力与历史习惯 | 调查 agent | 来源清楚，明确事实/推断，私人历史不发布 | 完成：22个会话+7个近期摘要；6个平台插件包、19项技能目录 |
| 归并到一个仓库 | 资产 agent | 保留原改动和许可证；可独立安装；重复安装与冲突可处理 | 归并、隔离安装、独立验收及真实入口切换通过 |
| 强化简短工作协议 | 行为 agent + 独立复核 | 普通任务直接做；保留源文件；核对当前结果；取消和外来消息不越权 | 早期部分合成场景通过；后续原生规划、接续与真实台账暴露缺口，独立业务交付能力未通过；见真实案例 |
| 安装本地 Hermes | 平台 agent | 固定版本可运行；原生任务能触发、留记录、暂停 | 已安装0.21.1；无模型调度/持久化通过；原生后台服务已运行 |
| 接入 GPT / MiMo 订阅 | 平台 agent | 使用原生入口；一次真实模型回复和本地工具调用 | GPT 已切换当前账号支持的 gpt-5.6-sol，真实回复及 TG 工具任务通过；MiMo 原生短文本及真实文件读写、回读核对通过，持续运行分别验收 |
| 接入 Telegram / 企业微信 | 平台 agent + 用户提供登录条件 | 从实际获准渠道收到一条信息，能够关联到待办 | 两个渠道本人私聊均完成真实只读工具任务及准确回复；企微为原生 WSS，当前配置只允许本人；群为空允许列表，完整入站拒绝行为尚未验收 |
| 修复已发现的阻碍 | 对应 owner | 只复验受影响部分，失败不冒充完成 | 早期已定位问题有定向修复与复验；真实 worker 失联恢复、迟到结果及完整渠道拒绝链路仍需按验收证据补齐，不能概括为全部阻碍已解决 |
| 将既有专项技能接入 Hermes | 能力 agent + 独立复核 | 原生发现、实际执行、产物回读、源输入保留 | xmind 已完成单项安装与真实 CLI 生成回读；其他专项技能按实际用途接入，不批量开放 |
| 发布唯一维护入口 | 主线程 | 安装、敏感信息和必要回归通过；明确未接通项 | 完成：已发布、干净克隆通过、真实入口切换、11 个旧仓归档 |

依赖顺序：归并/协议/本地安装可并行；订阅与渠道具备登录条件后接入；完成工作习惯到插件的落实与基础环境验收后，再按用户明确安排启用日常跟进。发布源码可以先完成，但不能宣称未接通的渠道已经可用。

## 第一件实际工作怎么验收

从获准消息或本地资料得到一项明确任务 → 调用现有能力执行 → 检查实际结果 → 给出简短交付或具体阻塞。重复信息不重复执行，未变化不刷屏，取消后停止。

已完成一次本地资料任务：原生 gateway 自动领取，读取实施计划与仓库状态，生成下一步摘要；输入哈希保持一致。首次内容验收发现重复确认和虚构整理工作，修正默认角色后同一任务复验通过。该任务使用接入 MiMo 前的计划快照，不能用其中的待接入描述判断当前订阅状态。

普通文件任务只做内容检查和源文件保留。涉及实际发布、付费、生产或重要数据时，才增加与影响相称的授权和独立验证。

## 接入前提

- GPT和MiMo订阅分别使用自身支持的认证方式；不把现有Codex登录误写成Hermes已登录。
- Telegram机器人只能获取机器人可见信息；个人已有历史需要账号客户端或导出方式。
- 企业微信客户端已登录；机器人/应用不能默认读取个人全部历史，会话存档取决于企业权限。现有Rico需核对API长连接、Bot ID/Secret与可使用成员；该模式不需要本机公网IPv4或回调域名。
- 私人记录、任务数据库和凭据留在本机；GitHub只保存代码、脱敏规则与样例。

详细调查保留在本机，不要求日常助手每次读取。后续状态见 [status.md](status.md)，Hermes操作见 [安装说明](../hermes/INSTALL.md)。
