# 实施状态

更新：2026-09-09。此文件记录代码版本的交付边界，不作为实时运行状态。

用户最新纠偏：下表的技术接入结果不代表整合目的已经满足。当前主线是从历史记录识别可接受结果与工作方式，完善项目预期和下发方案，再落实到 Hermes；统一集合与按需加载服务于这个目标。设计—验收流程的性能对比后置，日常监测继续暂停。详见[当前主线任务书](habits-plugin-rebuild.md)。

记忆方案采用 Hermes 自身的内置文件记忆、session 与原生 Kanban，不接入旧记忆插件或外部 provider。两类记忆和写入审批保持开启，自动后台复盘已关闭且原生读取结果确认。隔离环境 15 项存储、提示构建、审批暂存与跨实例/profile 检查通过；随后两次独立 MiMo 调用分别正确读取既存虚构偏好、在无记录时回答未知，均无工具调用。真实记忆和默认模型未修改；这证明原生读取，不替代实际工作满意度或团队隔离验收。

十八项冻结要求的实际覆盖范围见[当前验收证据](acceptance-results.md)，不以局部通过率代替整体完成判断。

| 阶段 | 当前结论 | 完成条件 |
|---|---|---|
| 现状调查 | 官方可行性和历史分析已落盘；资产清单完成 | 22个代表性会话与7个近期摘要，平台范围限制明确 |
| 统一源码与安装 | 6 个平台插件包、19 项技能目录已归并；先前迁移验收记录 41 个技能入口与 Codex/Claude 自建插件切换 | 原独立安装、冲突保留和卸载有历史证据；Claude 严格校验、用户级安装与缓存发现通过，未调用 Claude 模型；这些不自动证明新的模块筛选全部经过真实平台安装 |
| 统一管理入口 | `9dc1cea` 提供 list/install/update、平台与模块选择、简介和动态目录计数，Hermes 核心纳入 catalog | 34 项脚本回归和独立审查通过；真实 Hermes 核心+xmind 幂等通过，四份运行配置/任务文件未变。Codex/Claude 筛选与 Git 更新仍为隔离或受控验证；见[入口规格](unified-entry.md) |
| Hermes 成熟技能复用 | 补入明确点名的腾讯云技能；当前仓库 Hermes 目录为核心、XMind、腾讯云三项 | 腾讯云定向安装、重复安装、外来入口保护及原生发现通过；25 项相关现有测试通过。尚未执行腾讯业务操作，其他技能按用途分批选择 |
| 工作协议与验收 | 指定历史样本已形成六类可接受条件，核心与 Hermes SOUL 已接入按需预期/方案参考；原生任务实际自动读取，接续同一项目时能核对当前目录并区分原始目标与附加建议 | 两例历史回放、原生规划与接续均保留缺口，主线程修订不算新的行为通过。接续内容可复用，但产物落到旧目录，外部超时约束未生效；不能记整轮通过。见[行为对应表](habits-to-behavior.md)；跨会话记忆、协作接管与持续项目监督仍需用途级证据 |
| Hermes 本地部署 | 原生 launchd 服务运行；GPT 当前使用订阅可用的 gpt-5.6-sol | GPT 短调用和 Telegram 本地工具任务通过，工作场景流中断已定位到本地 12 秒阈值；临时 60 秒同场景单次复验通过，60 秒设置已持久加载，原生重启后两个渠道均重新连接；持续运行尚未验收；MiMo 原生文件读取、写入、回读通过，订阅路由已核对 |
| 自动日常工作闭环 | 后台资料任务、双渠道工具回复与企微一次性主动投递通过；按用户要求暂停日常监测 | 没有启用的定时任务；该状态不单独证明没有正在执行的任务；日常 job 未创建，一次性投递验证 job 已删除，旧安装巡检保留 paused；基础工作完成后再安排 |
| Telegram | 本人精确允许列表已启用，加入群关闭；真实 Git 查询收到准确回复 | 10:09 请求、10:10 最终回复，分支/工作区/提交与冻结仓库一致 |
| 企业微信 | 已按授权从旧 URL 回调切到原生长连接；通过真实消息确认本人并精确允许，配置为群允许列表且列表为空 | 真实工具任务回复与冻结 Git 事实一致；界面成员范围仅一人，Hermes 仍独立限制本人 |
| GitHub 唯一维护入口 | worklikerico/main 已发布并通过干净克隆；11 个旧仓已归档 | 唯一维护入口已收口；旧缓存与私有备份保留用于恢复 |

停止验证：Hermes 隔离进程测试中，真实 `/stop` 处理器终止了登记的等待进程，后续标记未生成；agent 和渠道适配器使用替身，不等于真实渠道完整验收。Codex 子 agent 取消试验阻止了预定的下一次工具调用，不代表已发生的外部效果可以撤销。

团队隔离：当前仅开放本人。不同 session 不等于记忆、文件、工具权限隔离；同一 Hermes profile 共享资源，团队开放尚未验收。授权负例测试请求被自动安全检查拒绝，未运行，只读配置核对不能替代行为测试。

不以安装成功代替模型可用，不以机器人可连接代替全部聊天历史可读，不以看板 done 代替独立验收。
